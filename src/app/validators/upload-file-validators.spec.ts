import { UploadFileValidators } from './upload-file-validators';

describe('UploadFileValidators', () => {
  it('should create an instance', () => {
    expect(new UploadFileValidators()).toBeTruthy();
  });
});
