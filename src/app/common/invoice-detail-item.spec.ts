import { InvoiceDetailItem } from './invoice-detail-item';

describe('InvoiceDetailItem', () => {
  it('should create an instance', () => {
    expect(new InvoiceDetailItem()).toBeTruthy();
  });
});
