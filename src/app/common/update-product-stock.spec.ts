import { UpdateProductStock } from './update-product-stock';

describe('UpdateProductStock', () => {
  it('should create an instance', () => {
    expect(new UpdateProductStock()).toBeTruthy();
  });
});
